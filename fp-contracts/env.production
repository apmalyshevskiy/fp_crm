# Этот файл Vite использует ТОЛЬКО при `npm run build` (прод).
# В проде прокси нет — фронт обращается к api.php напрямую, на том же домене.
# Путь к API от корня домена (api.php лежит в корне 139.100.204.216):
VITE_API_URL=
VITE_API_TOKEN=fpos_x7Kj9mNqR3vL2pWdF8sH-GIx9CIHFXjnw

# VITE_API_TOKEN берётся из основного .env (он общий для dev и prod).
# Basic-логин в прод-сборку НЕ зашивается — его спросит браузер при открытии сайта.
